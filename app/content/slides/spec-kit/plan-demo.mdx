---
title: Plan
order: 8
transition: fade
---

import { SlideCode } from '../../../components/SlideCode';

<div className="h-full flex flex-col justify-center gap-4 sm:gap-6">
    <div>
        <div className="uppercase tracking-[0.3em] text-[0.65rem] sm:text-xs text-secondary-400 mb-3 sm:mb-4">07 / The how</div>
        <h2 className="font-black text-[clamp(1.75rem,4vw+0.5rem,4.5rem)] text-white leading-[1.05]">Now name the stack.</h2>
    </div>
    <SlideCode
        language="md"
        code={`/speckit.plan

The application uses Vite with minimal number of libraries.
Use vanilla HTML, CSS, and JavaScript as much as possible.
Images are not uploaded anywhere and metadata is stored
in a local SQLite database.

/speckit.tasks       # break the plan into ordered steps
/speckit.implement   # the agent writes the code`}
    />
    <div className="text-zinc-400 text-[clamp(0.85rem,1vw+0.2rem,1.2rem)]">Plan → Tasks → Implement. Every step writes a Markdown artifact the next step can read.</div>
</div>
