---
title: clarify
order: 13
transition: fade
---

import { SlideCode } from '../../../components/SlideCode';

<div className="h-full flex flex-col justify-center gap-4 sm:gap-6">
    <div>
        <div className="uppercase tracking-[0.3em] text-[0.65rem] sm:text-xs text-secondary-400 mb-3 sm:mb-4">12 / clarify</div>
        <h2 className="font-black text-[clamp(1.75rem,4vw+0.5rem,4.5rem)] text-white leading-[1.05]">The agent asks what you forgot.</h2>
    </div>
    <SlideCode
        language="bash"
        code={`/speckit.clarify

? Should albums be shareable with other users?  (y/N)
> N

? When storage fills, what happens?
  1. Warn at 90%, hard-stop at 100%
> 2. Auto-archive oldest album at 95%
  3. Both

? Minimum supported viewport width?
> 360px

? Drag-and-drop on touch devices?  (Y/n)
> Y`}
    />
    <div className="text-zinc-400 text-[clamp(0.85rem,1vw+0.2rem,1.2rem)]">Better to answer once than to guess twice. Every answer lands in the spec.</div>
</div>
