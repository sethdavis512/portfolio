---
title: The Boundary
order: 4
transition: fade
---

import { SlideCode } from '../../../components/SlideCode';

<div className="h-full flex flex-col justify-center gap-4 sm:gap-6">
    <div>
        <div className="uppercase tracking-[0.3em] text-[0.65rem] sm:text-xs text-secondary-400 mb-3 sm:mb-4">03 / The boundary</div>
        <h2 className="font-black text-[clamp(1.75rem,4vw+0.5rem,4.5rem)] text-white leading-[1.05]">A spec is a boundary.</h2>
    </div>
    <SlideCode
        language="md"
        code={`# .specify/memory/constitution.md
- Data fetching uses TanStack Query. Never bare fetch.
- Tests describe behavior, not implementation.
- No new top-level dependencies without an ADR.

# specs/users-pagination/spec.md
/speckit.specify

Add pagination to the users list. Preserve the existing
component shape. Preserve the existing tests.`}
    />
    <div className="text-zinc-400 text-[clamp(0.85rem,1vw+0.2rem,1.2rem)]">The agent reads both. The diff stays the size you expected. The "why" is on disk for the next person — human or agent.</div>
</div>
