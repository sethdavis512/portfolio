---
title: Ship as a Binary
order: 6
transition: lift
---

import { SlideCode } from '../../../components/SlideCode';

<div className="h-full flex flex-col justify-center gap-4 sm:gap-6">
    <div>
        <div className="uppercase tracking-[0.3em] text-[0.65rem] sm:text-xs text-primary-400 mb-3 sm:mb-4">05 / The ship</div>
        <h2 className="font-black text-[clamp(1.75rem,4vw+0.5rem,4.5rem)] text-white leading-[1.05]">One command. One binary.</h2>
    </div>
    <SlideCode
        language="bash"
        code={`bun build --compile ./cli.ts --outfile my-cli

./my-cli    # runs without Bun, without Node, anywhere.`}
    />
    <div className="grid grid-cols-1 sm:grid-cols-3 gap-3 sm:gap-4 text-sm">
        <div className="rounded-lg border border-zinc-800 bg-zinc-900/60 p-4 sm:p-5">
            <div className="font-mono text-primary-400 mb-1">~50MB</div>
            <div className="text-zinc-400">Self-contained runtime</div>
        </div>
        <div className="rounded-lg border border-zinc-800 bg-zinc-900/60 p-4 sm:p-5">
            <div className="font-mono text-secondary-400 mb-1">zero</div>
            <div className="text-zinc-400">Install steps for users</div>
        </div>
        <div className="rounded-lg border border-zinc-800 bg-zinc-900/60 p-4 sm:p-5">
            <div className="font-mono text-tertiary-400 mb-1">rsync</div>
            <div className="text-zinc-400">Push it to any box</div>
        </div>
    </div>
</div>
