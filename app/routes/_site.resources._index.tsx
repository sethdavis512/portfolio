import { BORDER_COLOR } from '~/constants';

export default function ResourcesIndexRoute() {
    return (
        <div
            className={`flex items-center justify-center rounded-lg ${BORDER_COLOR} p-8`}
        >
            <p>Select a page above...</p>
        </div>
    );
}
