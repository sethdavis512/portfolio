query GetPackages {
    packages(orderBy: { order: asc }) {
        id
        name
        description
        type
        order
        offerings {
            id
            name
            description
        }
        price
    }
}
