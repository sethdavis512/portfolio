query GetPost($where: PostWhereUniqueInput!) {
    post(where: $where) {
        id
        slug
        title
        content {
            document
        }
        author {
            name
            id
        }
    }
}
