query GetTILBySlug($slug: String!) {
    post(where: { slug: $slug }) {
        id
        slug
        title
        createdAt
        updatedAt
        content {
            document
        }
        tags {
            id
            name
        }
    }
}
