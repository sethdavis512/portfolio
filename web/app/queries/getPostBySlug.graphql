query GetPostBySlug($slug: String!) {
    post(where: { slug: $slug }) {
        id
        slug
        title
        content {
            document
        }
        author {
            name
            id
        }
    }
}
